import React from "react";

const VoteClosed = (props) => {
    return (
        <div className="login-container">
            <h1 className="welcome-message">Voting is closed!</h1>
        </div>
    )
}

export default VoteClosed;